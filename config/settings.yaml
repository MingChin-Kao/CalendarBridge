# Calendar Sync Configuration

# Source ICS configuration
source:
  url: "https://msx.itri.org.tw/OWA/calendar/f55abdc79f004d60b8fa0d00d5a3462f@itri.org.tw/f5e56d38476148e1a4e703c72d04b0be10407649746860206970/calendar.ics"
  timeout: 30
  retry_count: 3
  user_agent: "Calendar-Sync/1.0"

# Google Calendar configuration
google_calendar:
  calendar_id: "879df34e2d1e2756717236682658c9c81a80e2a0005d1e9eda9b37dade6b087c@group.calendar.google.com"  # 或指定特定的行事曆 ID
  credentials_file: "config/credentials.json"
  token_file: "config/token.json"
  application_name: "Calendar Sync Tool"

# Sync configuration
sync:
  interval_minutes: 30  # 同步間隔 (分鐘)
  max_events_per_batch: 100
  lookahead_days: 365  # 向前同步天數
  lookbehind_days: 30   # 向後同步天數 (處理修改)
  enable_delete: true   # 是否刪除來源已移除的事件
  conflict_resolution: "latest"  # latest, source, manual

# Database configuration
database:
  path: "data/sync_state.db"
  backup_count: 5

# Logging configuration
logging:
  level: "INFO"  # DEBUG, INFO, WARNING, ERROR
  file: "logs/calendar_sync.log"
  max_size_mb: 10
  backup_count: 5
  console: true

# Event processing
processing:
  timezone: "Asia/Taipei"
  event_prefix: "[ITRI] "  # 事件標題前綴
  description_suffix: "\n\n--- 由 Calendar Sync 工具同步 ---"
  max_description_length: 8000